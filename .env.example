# Copy this to backend/.env for local development
PORT=3001
JWT_SECRET=vervang-dit-met-een-lang-willekeurig-wachtwoord
NODE_ENV=development
# DATABASE_PATH=/data/ptapp.db   # Only needed on Railway (persistent volume)
